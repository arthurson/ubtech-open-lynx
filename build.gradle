// Root build file. Pins match the upstream Alpha2OpenSdk repo exactly (see its
// AGENTS.md / build.gradle) so the SDK module builds unmodified against them.
plugins {
    id 'com.android.application' version '4.2.2' apply false
    id 'com.android.library' version '4.2.2' apply false
}
